[phases.setup]
nixPkgs = ['...', 'python3']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Build phase completed"']

[start]
cmd = 'uvicorn pinecone_backend:app --host 0.0.0.0 --port $PORT'
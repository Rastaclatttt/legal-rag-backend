[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
startCommand = "uvicorn pinecone_backend:app --host 0.0.0.0 --port $PORT"

[env]
PORT = "8000"
PYTHONPATH = "."